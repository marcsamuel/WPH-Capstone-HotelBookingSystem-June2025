// Login form logic
const loginForm = document.getElementById('login-form');
loginForm.addEventListener('submit', function(e) {
  e.preventDefault();
  // Authenticate with backend (to be implemented)
  alert('Login submitted!');
});
